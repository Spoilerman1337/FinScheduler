apiVersion: v1
kind: ConfigMap
metadata:
  name: finscheduler-api-config
data:
  ENV: LocalKubernetes
  SERVER_PORT: "8080"
  SERVICE_NAME: fin-scheduler-api
  CORS_ALLOWED_ORIGINS: "*"
  CORS_ALLOWED_METHODS: GET,POST,PUT,DELETE,OPTIONS
  CORS_ALLOWED_HEADERS: "*"
  CORS_ALLOW_CREDENTIALS: "false"
