apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: mimir-config
    files:
      - mimir.yaml=mimir.yaml
generatorOptions:
  disableNameSuffixHash: true
