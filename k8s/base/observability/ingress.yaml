apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finscheduler-observability
spec:
  rules:
    - host: grafana.finscheduler.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
